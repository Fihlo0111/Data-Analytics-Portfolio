CREATE TABLE Products
(
    ProductID INT IDENTITY(1,1) PRIMARY KEY,
    ProductName VARCHAR(150) NOT NULL,
    CategoryID INT NOT NULL,
    UnitPrice DECIMAL(10,2) NOT NULL,

    CONSTRAINT FK_Product_Category
        FOREIGN KEY(CategoryID)
        REFERENCES Categories(CategoryID)
);

----inserting data into the table 

INSERT INTO Products
(
ProductName,
CategoryID,
UnitPrice
)

VALUES

('Dell Laptop',1,15999.99),
('HP Laptop',1,13499.99),
('Wireless Mouse',1,399.99),
('Mechanical Keyboard',1,999.99),

('Office Chair',3,2499.99),
('Wooden Desk',3,3999.99),

('White Bread',2,18.99),
('Long Life Milk',2,22.99),

('Nike Running Shoes',5,1799.99),

('Men T-Shirt',4,249.99);

----checking data
select * from Products
